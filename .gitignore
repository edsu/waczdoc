# dependencies
node_modules/

# output directories. -o takes an arbitrary name, so these patterns only cover
# the conventional ones -- check `git status` before adding.
out/
out2/
pdfs/
*.pdf
markdown/
*markdown*/

# local editor/agent settings
.claude/settings.local.json

# macOS
.DS_Store

# large local archives (keep tiny test fixtures under fixtures/)
*.wacz
!fixtures/*.wacz

# TypeScript build output
dist/
